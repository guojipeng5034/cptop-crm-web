import Utils from 'remote-sys-base/Utils';

export const {
  hasClass,
  addClass,
  removeClass,
  getBoundingClientRect,
  getViewportOffset,
  on,
  off,
  once,
  getStyle,
  setStyle,
  isScroll,
  getScrollContainer,
  isInContainer,
  ViewportOffsetResult,
} = Utils.domUtils;
